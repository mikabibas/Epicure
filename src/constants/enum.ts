export enum ERestaurantStatus {
  IDLE = "idle",
  LOADING = "loading",
  SUCCEEDED = "succeeded",
  FAILED = "failed",
}

export enum EAppRoutes {
  HOMEPAGE = "/",
  RESTAURANTS = "/restaurants",
  CHEFS = "/chefs",
}
